import { DateBorderdDirective } from './date-borderd.directive';

describe('DateBorderdDirective', () => {
  it('should create an instance', () => {
    const directive = new DateBorderdDirective();
    expect(directive).toBeTruthy();
  });
});
